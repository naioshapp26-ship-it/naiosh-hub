{
  "name": "naiosh-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Naiosh Hub 360 — Imperial Operations Platform",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "db:migrate": "node -e \"require('./db/migrate').migrate().then(r=>{console.log(r);process.exit(0)}).catch(e=>{console.error(e);process.exit(1)})\"",
    "env:check": "node -e \"console.log(JSON.stringify(require('./db/env-check').checkEnvironment(),null,2))\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
